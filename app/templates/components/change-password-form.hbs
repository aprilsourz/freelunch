




{{#paper-form onSubmit=(action "submit")}}
  {{paper-input
      required=true
      label="Old Password"
      type="password"
      value=passwords.previous
      onChange=(action (mut passwords.previous)) }}
  {{paper-input
      required=true
      label="New Password"
      type="password"
      value=passwords.next
      onChange=(action (mut passwords.next)) }}
  {{#paper-button type='submit'}}Submit{{/paper-button}}
{{/paper-form}}
