{{#paper-form onSubmit="createMessage"}}
  {{paper-input
    textarea=true
    block=true
    label="Type message here"
    maxlength=750
    passThru=(hash rows=4 maxRows=4)
    value=newMessage.body
    onChange=(action (mut newMessage.body))
    required=true
  }}
  {{#paper-button }}Send{{/paper-button}}
{{/paper-form}}
