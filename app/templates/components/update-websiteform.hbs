<form {{action 'updateUrl' on='submit'}} class='form-group'>
  <label for="kind">Website URL</label>
  {{input type="url"
          maxlength=100
          required="true"
          id="url"
          placeholder="URL"
          value=url}}
  <button type=submit class="btn btn-default">Save</button>

</form>
