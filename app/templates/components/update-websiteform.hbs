
  {{#paper-form onSubmit=(action "updateUrl")}}

    {{paper-input
      label="URL"
      maxlength=100
      type="url"
      value=url
      onChange=(action (mut url))
      required=true
    }}
  {{#paper-button type=submit}}Save {{/paper-button}}
{{/paper-form}}
