<div class="row">
  <div class="col-md-3"></div>
  <div class="col-md-6">

  <h3>{{conversation.recruiter.name}} has invited you to lunch! <br/> View Website: <a target="_blank" href="{{conversation.recruiter.website}}">{{conversation.recruiter.website}}</a></h3>


<p> You may only send one response to this invitation. <br/>
  Be sure to include your email address if you want to have lunch with them.
</p>

<h5>Recruiter's message</h5>
<pre>{{conversation.lunchRequest}}</pre>


{{#if conversation.isCompleted}}
<h5>Your response</h5>
<pre>{{conversation.response}}</pre>
{{else}}
<form class="form-group" {{action 'reply' on='submit'}}>
<fieldset>Message must be under 1000 characters.</fieldset>
{{textarea
    required="true"
    maxlength="1000"
    cols="50"
    rows="6"
    class="form-control"
    value=text
    placeholder='Type Resonse Here'
  }}
  <button type=submit class="btn btn-primary">Send</button>
</form>
{{/if}}
{{#link-to 'engineer.conversations'}}<button class="btn btn-primary">Back</button>{{/link-to}}

</div>
<div class="col-md-3"></div>
</div>
