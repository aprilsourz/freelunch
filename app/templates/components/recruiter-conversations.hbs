<thead>
  <tr>
    <th>Name</th>
    <th>Status</th>
  </tr>
</thead>
<tbody>
  {{#each conversations as |conversation|}}
    {{recruiter-conversations/conversation deleteConvo='deleteConvo' conversation=conversation}}
  {{/each}}
</tbody>

{{#if conversations}}

{{else}}
<h4>You haven't invited anyone to lunch yet</h4>
{{/if}}
