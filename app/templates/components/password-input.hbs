<label for="password">Password</label>
{{input type="password"
        maxlength=20
        required="true"
        class="form-control"
        id="password"
        placeholder="Password"
        value=password}}
